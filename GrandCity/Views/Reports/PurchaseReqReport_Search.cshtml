
@model IEnumerable<MeherEstateDevelopers.Models.Sp_Get_PurchaseReq_Report_Result>


<style>
    table thead tr th, table tbody tr td {
        font-size: 12px;
    }
</style>

<table id="pr-list" class="table table-striped table-bordered " cellspacing="0" width="100%" style="margin-left:0%">
    <thead class="thead-dark">
        <tr>
            <th>Sr.</th>
            <th>Name</th>
            <th>PR NO</th>
            <th>Applied Date</th>
            <th>Approved Date</th>
            <th>Department</th>
            <th>Project</th>
            <th>Status</th>
        </tr>
    </thead>

    <tbody>
        @{
            if (Model.Any())
            {
                int i = 1;
                foreach (var item in Model)
                {
                    <tr id="">
                        <td>@i</td>
                        <td>@item.Item_Name</td>
                        <td>@item.Requisition_No</td>
                        <td>@item.DateTime.Value.ToShortDateString()</td>
                        @if (item.ApprovedBy_Date == null)
                        {
                            <td></td>
                        }
                        else
                        {
                            <td>@item.ApprovedBy_Date.Value.ToShortDateString()</td>

                        }

                        <td>@item.Dep_Name</td>
                        <td>@item.Prj_Name</td>
                        <td>@item.Status</td>
                    </tr>
                    i++;
                }
            }
        }

    </tbody>
</table>

<script>

    $(document).ready(function () {
        $('#pr-list').DataTable({
            dom: 'Bfrtip',
            buttons: [
                'copy', 'excel', 'pdf', 'print'
            ]
        });
    });
</script>

<style>
    tr.group,
    tr.group:hover {
        background-color: #ddd !important;
    }
</style>
