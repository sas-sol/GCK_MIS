@*@model IEnumerable<MeherEstateDevelopers.Models.Sp_Get_All_Employess_Salaries_Result>

@{ Layout = "~/Views/Shared/_StaffLayout.cshtml"; }

<div class="col-md-12">
    <div class="bgc-white bd bdrs-3 p-20 mB-20">
        <h4 class="c-grey-900 mB-20">Employess salaries</h4>
        <table id="salary-dat" class="table table-striped table-bordered " cellspacing="0" width="100%">
            <thead class="">
                <tr>
                    <th>Employee Code</th>
                    <th>Emloyee Name</th>
                    <th>Employee Designation</th>
                    <th>Date</th>
                    <th>Basic Salary</th>
                    <th>Allownces & Bonus</th>
                    <th>Deductions & Tax</th>
                    <th>Gross Total</th>
                    <th>Remarks</th>
                    <th>Edit</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                @{
                    foreach (var item in Model)
                    {

                        <tr id="@item.Id">
                            <td align="center" class="col-mod">@item.Employee_code</td>
                            <td align="center" class="col-mod">@item.Employee_Name</td>
                            <td align="center" class="col-mod">@item.Employee_Designation</td>
                            <td align="center" class="col-mod ">@string.Format("{0: MM/yyyy}", item.Salary_Month)</td>
                            <td align="center" class="col-mod s-read">@String.Format("{0:n0}", item.Basic_salary)</td>
                            <td align="center" class="col-mod t_allownce"> @String.Format("{0:n0}", item.Allownces) </td>
                            <td align="center" class="col-mod">@String.Format("{0:n0}", item.Deductions)</td>
                            <td align="center" class="col-mod GrndTot" style="font:bold">@String.Format("{0:n0}", item.Grand_total)</td>
                            <td align="center" class="col-mod "><input type="text" class="salary_remarks" placeholder="Enter reason" value="@item.Remarks"/></td>
                            <td data-toggle="modal" data-target="#Modal" v class="ti-pencil Salary_details"><input type="hidden" value="@item.Id" id="salary_id_get" /></td>
                            <td align="center" class="col-mod">
                            <input type="checkbox" id="InProcess" onchange="" required="required" class="peer Salary_Status" value="">
                              
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

<h3>Grand Total</h3>
<div><h1>@String.Format("{0:n0}", Model.Sum(x => x.Grand_total))</h1>  </div>


<script>

    $(document).ready(function () {
        $('#salary-dat').DataTable({
            "scrollX": true,
            dom: 'Bfrtip',
            buttons: [
                'copy', 'csv', 'excel', 'pdf', 'print'
            ]

        });
    });
</script>*@

